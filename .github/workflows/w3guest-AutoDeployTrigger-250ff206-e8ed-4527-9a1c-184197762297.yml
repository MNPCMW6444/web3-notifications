name: Trigger auto deployment for w3guest

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ guest ]
    paths:
    - '**'
    - '.github/workflows/w3guest-AutoDeployTrigger-250ff206-e8ed-4527-9a1c-184197762297.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.W3GUEST_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: w3notif.azurecr.io
          registryUsername: ${{ secrets.W3GUEST_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.W3GUEST_REGISTRY_PASSWORD }}
          containerAppName: w3guest
          resourceGroup: braikupcluster
          imageToBuild: w3notif.azurecr.io/guest:${{ github.sha }}
          
            

     

